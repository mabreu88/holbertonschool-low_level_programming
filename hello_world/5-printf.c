#include<stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 (success)
 */
int main(void)
{
/*
 * Use printf to print the line, printf is a function included in the stdio.h
 * lib.
 */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
