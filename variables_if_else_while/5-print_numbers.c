#include<stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int x;
/*
 * With for structure increse x each time and print it
 */
	for (x = 0; x < 10; x++)
		printf("%i", x);
	printf("\n");
return (0);
}
